# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo

# Aligner / pipeline working files (regenerated locally)
pipeline/*.json
pipeline/*.txt
!pipeline/book12_chapters.txt
!pipeline/book12_track_map.json
sync*.json
align*.json
volume*.mp3

# Wayback raw-HTML snapshot cache (fetch_text.py --wayback): cached author prose,
# never tracked — keep the whole cache dir out regardless of where it's pointed.
_wb_cache/
*_wb_cache/

# Your audiobook source + working audio (never committed — copyright + size)
Reading/
*.m4a
*.m4b
*.wav
samples/
